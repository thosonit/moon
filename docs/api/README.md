# API

N/A for this project. `moon` has no server or API layer — pages fetch static JSON files directly from `src/moon/data/` (see `data/topics.json` and `data/<topicId>.json`). No `api.md` or `endpoints/` docs are needed.
