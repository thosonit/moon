# Database

N/A for this project. `moon` has no database — lesson content lives in static JSON files under `src/moon/data/` (`topics.json` plus one `<topicId>.json` per topic). No schema, ERD, or migration notes apply here.
